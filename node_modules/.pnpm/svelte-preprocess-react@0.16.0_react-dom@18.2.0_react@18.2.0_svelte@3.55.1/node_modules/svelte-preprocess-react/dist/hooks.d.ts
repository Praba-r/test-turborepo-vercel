import { type Readable } from "svelte/store";
import type ReactDOMServer from "react-dom/server";
export default function hooks<T>(callback: () => T, ReactDOMClient?: any, renderToString?: typeof ReactDOMServer.renderToString): Readable<T | undefined>;
